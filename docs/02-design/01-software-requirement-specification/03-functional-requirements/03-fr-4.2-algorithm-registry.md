# FR §4.2 — Algorithm Registry

*Registers, configures, and manages Algorithm Implementations. Enforces the Algorithm / Algorithm Instance / Implementation distinction.*
*Source: MANIFESTO Principles 8–11.*
*Use Cases: UC-02.*

---

## FR-05

**The system MUST store Algorithm Instance records conforming to the schema defined in `docs/03-technical-contracts/01-data-format.md` §2.2, including: `id` (UUID), `name`, `algorithm_family`, `hyperparameters`, `configuration_justification`, `code_reference` (version-pinned), `language`, `framework`, `framework_version`, `known_assumptions`, and provenance fields.**

- **Priority:** MUST
- **Source:** MANIFESTO Principles 8, 10
- **Accepted when:** An `AlgorithmInstance` record with all 12 required fields is stored and retrievable; a registration with any required field absent raises `ValidationError` naming the missing field.
- Connects to: `docs/03-technical-contracts/01-data-format.md` §2.2; `docs/03-technical-contracts/02-interface-contracts.md` Algorithm interface

## FR-06

**The system MUST reject Algorithm Instance registration if the `code_reference` is not resolvable or is not version-pinned. An unpinned reference (e.g., a branch name rather than a commit hash or package version) MUST be rejected with an explicit error.**

- **Priority:** MUST
- **Source:** MANIFESTO Principles 8, 19
- **Accepted when:** `code_reference="git+https://github.com/org/repo@main"` raises `CodeReferenceError` (floating branch); `code_reference="git+https://github.com/org/repo@abc1234"` and `code_reference="scipy==1.13.0"` are each accepted.
- Connects to: `docs/03-technical-contracts/01-data-format.md` §2.2 validation rules

## FR-07

**The system MUST require a non-empty `configuration_justification` for every Algorithm Instance. A registration with an empty or missing justification MUST be rejected.**

- **Priority:** MUST
- **Source:** MANIFESTO Principle 10 (configuration fairness)
- **Accepted when:** `configuration_justification=""` and `configuration_justification=None` each raise `ValidationError`; a record with a non-empty justification string is accepted.
- Connects to: `docs/03-technical-contracts/01-data-format.md` §2.2
